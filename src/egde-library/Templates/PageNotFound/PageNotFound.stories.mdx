import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Root, DocsPageHeader } from 'egde-library';
import LinkTo from '@storybook/addon-links/react';
import * as stories from './PageNotFound';

export const templateName = 'Page Not Found';

<Meta title={Root.Templates + templateName} />

<DocsPageHeader
    title={templateName}
    description={
        <p>
            This template shows how the{' '}
            <LinkTo kind="components-containers-pagecontainer" story="base">
                PageContainer
            </LinkTo>{' '}
            and{' '}
            <LinkTo kind="components-form-button" story="base">
                Button
            </LinkTo>{' '}
            component can be used to create a "Page Not Found" overlay.
        </p>
    }
    addImportantNote={false}
    variant="none"
/>

<Canvas withToolbar>
    <Story name="Page Not Found" story={stories.Base} />
</Canvas>

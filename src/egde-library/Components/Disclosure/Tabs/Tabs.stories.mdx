import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { LibPath, DocsPageHeader, DocsPageBodyComponents } from 'egde-library';
import { Tabs } from 'egde-components';
import * as PropItems from './PropItems';

export const Template = (args) => <Tabs {...args} />;

export const componentName = 'Tabs';

<Meta
    title={LibPath.Disclosure + 'Tabs'}
    component={Tabs}
    args={{
        tabs: PropItems.Default,
        centerTabs: false,
    }}
/>

<DocsPageHeader
    title={componentName}
    description="This component is used to display and hide content in distinctive tabs."
    addImportantNote={false}
    variant="stable"
/>

<Canvas withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Canvas>

<DocsPageBodyComponents addStories={true} />

### Many Tabs

<Canvas withToolbar>
    <Story
        name="Many Tabs"
        args={{
            tabs: PropItems.ManyTabs,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Tab Alignment: Center

<Canvas withToolbar>
    <Story
        name="Tab Alignment: Center"
        args={{
            centerTabs: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

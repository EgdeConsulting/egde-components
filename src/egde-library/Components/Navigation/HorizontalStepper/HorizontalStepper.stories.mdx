import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { LibPath, DocsPageHeader, DocsPageBodyComponents } from 'egde-library';
import { HorizontalStepper } from 'egde-components';
import * as PropItems from './PropItems';
import { useState } from 'react';
import { Button } from '@chakra-ui/react';

export const Template = (args) => {
    const [activeStep, setActiveStep] = useState(0);
    return (
        <div>
            <HorizontalStepper
                {...args}
                setActiveStep={setActiveStep}
                activeStep={activeStep}
            />
        </div>
    );
};

export const componentName = 'HorizontalStepper';

<Meta
    title={LibPath.Navigation + 'HorizontalStepper'}
    component={HorizontalStepper}
    args={{
        activeStep: 0,
        setActiveStep: () => {},
        stepsContent: PropItems.Default,
        buttonTexts: {
            previous: 'previous',
            next: 'next',
            complete: 'complete',
        },
        finalStep: {
            finalText: 'finalText',
            finalButtonText: 'fintalButtonText',
        },
        buttonAlignment: 'left',
        buttonMargin: '5px',
        clickableSteps: false,
        completedSteps: [],
        onFinalize: () => {},
        buttonCaption: {
            defaultValue: {
                text: 'Placeholder',
                color: '#ffffff',
            },
            onFinalStep: () => {},
            isCompleteButtonDisabled: true,
        },
    }}
/>

<DocsPageHeader
    title={componentName}
    description="This component is used to create a horizontal step-overview which shows the user the step he's on."
    addImportantNote={false}
    variant="stable"
/>

<Canvas withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Canvas>

<DocsPageBodyComponents addStories={true} />

### Completed Steps

<Canvas withToolbar>
    <Story
        name="Completed Steps"
        args={{
            activeStep: 4,
            completedSteps: [0, 1, 2, 3, 4],
            isCompleteButtonDisabled: false,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Clickable Steps

<Canvas withToolbar>
    <Story
        name="Clickable Steps"
        args={{
            clickableSteps: true,
            isCompleteButtonDisabled: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Many Steps

<Canvas withToolbar>
    <Story
        name="Many Steps"
        args={{
            stepsContent: PropItems.ManySteps,
            isCompleteButtonDisabled: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Button Alignment: Right

<Canvas withToolbar>
    <Story
        name="Button Alignment: Right"
        args={{
            buttonAlignment: 'right',
            isCompleteButtonDisabled: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

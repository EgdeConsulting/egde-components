import { Meta, Canvas } from '@storybook/addon-docs';
import { Root } from 'Types';

<Meta title={Root.GettingStarted + 'Testing'} />

# Testing ✅

<hr />

Component testing is done by running predefined Jest-tests inside of your project. For this component
library execute `yarn test` in order to run all of the tests.

import { Meta, Canvas } from '@storybook/addon-docs';
import { Root } from "./LibPath";

<Meta title = {Root.Main + "Startpage"} parameters = {{ previewTabs: { canvas: { hidden: true } } }} />

Instructions, Changelog, etc. can be added to .mdx-files like this one. 
import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DocsPageHeader, DocsPageBodyComponents } from 'Docs';
import { LibPath } from 'Types';
import { ContentContainer} from 'Components';

export const Template = (args) => <ContentContainer {...args} />

export const componentName = 'ContentContainer';


<Meta
    title={LibPath.Containers + componentName}
    component={ContentContainer}
    args={{
        children: 'Sample text goes here'
    }}
/>

<DocsPageHeader
    title={componentName}
    description="This component shows the use of a contentContainer, used for building websites."
    addImportantNote={false}
    variant="stable"
/>


<Canvas withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Canvas>

<DocsPageBodyComponents addStories={false} />


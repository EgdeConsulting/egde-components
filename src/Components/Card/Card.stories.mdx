import {
    Meta,
    Story,
    Props,
    Canvas,
    Preview,
} from '@storybook/addon-docs/blocks';
import { Card } from './Card';
import { ComponentStability } from 'Icons';
import { LibPath } from 'Types';

export const Template = (args) => <Card {...args} />;

<Meta
    title={LibPath.Cards + 'Card'}
    component={Card}
    args={{
        title: 'Title',
        textColors: '#D23F47',
        titleTextColor: '#000000',
        bodyTextColor: '#000000',
        titleTextSize: '14px',
        bodyTextSize: '14px',
        onClick: () => {},
        titleIcon: undefined,
        actionIcon: undefined,
        cardHeight: '',
        cardWidth: '',
        body: 'And a body ',
    }}
/>

# Card

<ComponentStability variant="stable" />

<hr />

This component is used to create a card-like container which can hold information like a title and text.

<Preview withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Preview>

Change some of the props and see how the `Base` component changes.

<Props story="Base" />

# Stories

<hr />

Below are shown different implementations of the component in regards to varying
use cases.

<br />

<Canvas withToolbar>
    <Story
        name="Customer information"
        args={{
            title: 'Ola Nordmann',
            body: 'Age: 420',
            cardHeight: '100px',
            cardWidth: '200px',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DocsPageHeader, DocsPageBody } from 'Docs';
import { LibPath } from 'Types';
import { Button } from 'Components';

export const Template = (args) => <Button {...args} />;

export const componentName = 'Button';

<Meta
    title={LibPath.Form + componentName}
    component={Button}
    args={{
        label: 'Button',
        variant: 'primary',
    }}
    argtypes={{
        onClick: { action: 'clicked' },
    }}
/>

<DocsPageHeader
    title={componentName}
    description="This component is used to create a button which the user can click in order to initiate an action."
    addImportantNote={false}
    variant="stable"
/>

<Canvas withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Canvas>

<DocsPageBody addStories={true} />

### Secondary Button

<Canvas withToolbar>
    <Story
        name="Secondary Button"
        args={{
            variant: 'secondary',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Delete Button

<Canvas withToolbar>
    <Story
        name="Delete Button"
        args={{
            variant: 'delete',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Icon Button

<Canvas withToolbar>
    <Story
        name="Icon Button"
        args={{
            variant: 'iconButton',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Icon and Text Button

<Canvas withToolbar>
    <Story
        name="Icon and Text Button"
        args={{
            variant: 'iconAndTextButton',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Link Button

<Canvas withToolbar>
    <Story
        name="Link Button"
        args={{
            variant: 'link',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

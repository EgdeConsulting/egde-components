import { Meta, Story, Props, Canvas, Preview } from '@storybook/addon-docs';
import { DocsPageHeader, DocsPageBody } from 'Docs';
import { FileInput } from './FileInput';
import { LibPath } from 'Types';

export const Template = (args) => <FileInput {...args} />;

export const componentName = 'FileInput';

<Meta
  title={LibPath.Form + componentName}
  component={FileInput}
  parameters={{
    toolbar: {
      title: { hidden: true },
    },
}}
args={{
    label: 'FileInput',
    labelTextMargin: '0px',
    invalidText: '',
    captionText: '',
    margin: '0px',
    padding: '0px',
    width: '100%',
    minWidth: '0px',
    size: 'md',
    variant: 'default',
    uploadLabel: 'Drag and drop or click to upload',
    dropLabel: 'Drop',
    maxFileSizeLabel: 'Max file size is....',
    maxUploadSizeLabel: 'Max upload size is...',
    maxFileSize: 1000,
    maxUploadSize: 1000,
    disabled: false,
    showIcon: true,
    captionTextMargin: '0px',
    maxWidth: '100%',
  }}
/>

<DocsPageHeader
    componentName={componentName}
    componentDescription="This component is used for file upload and input"
    addImportantNote={false}
    variant="stable"
/>

<Canvas withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Canvas>

<DocsPageBody addStories={true} />

<Canvas>
    <Story
        name="Invalid upload"
        args={{
            invalidText: 'Invalid upload',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Upload without icon

<Canvas>
    <Story
        name="Caption"
        args={{
            showIcon: false,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Disabled file upload

<Canvas>
    <Story
        name="Disabled"
        args={{
            variant: 'disabled',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Size variations

### Small

<Canvas>
    <Story
        name="Small"
        args={{
            size: 'sm',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Medium

<Canvas>
    <Story
        name="medium"
        args={{
            size: 'md',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

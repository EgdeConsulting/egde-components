import {
    Meta,
    Story,
    Props,
    Canvas,
} from '@storybook/addon-docs';
import { NumberInput } from './NumberInput';
import { LibPath } from 'Types';
import { DocsPageHeader, DocsPageBody } from 'Docs';

export const Template = (args) => <NumberInput {...args} />;

export const componentName = 'NumberInput'

<Meta
    title={LibPath.Form + componentName}
    component={NumberInput}
    parameters={{
        toolbar: {
            title: { hidden: true },
            // zoom: { hidden: true },
            // eject: { hidden: true },
            // copy: { hidden: true },
            // fullscreen: { hidden: true },
            // 'storybook/background': { hidden: true },
            // 'storybook/viewport': { hidden: true },
        },
    }}
    args={{
        label: 'Number Input',
        labelTextMargin: '0',
        value: '',
        maxValueLength: 15,
        onChange: () => {},
        invalidText: '',
        isDisabled: false,
        placeholder: 'Placeholder',
        captionText: '',
        isCurrency: false,
        currencySpacer: ' ',
        margin: '0',
        padding: '0',
        width: '100%',
        minWidth: '0',
        backgroundColor: '#ffffff',
        autoFocus: false,
        textAlign: 'right',
        allowNegative: false,
        captionTextMargin: '0',
        maxWidth: '100%',
        maxValue: '10',
    }}
/>

<DocsPageHeader
    componentName={componentName}
    componentDescription="This Component is used when you are supposed to only use numberinput"
    addImportantNote={false}
    variant="stable"
/>

<Canvas withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Canvas>


<DocsPageBody addStories={true} />

### Disable number input

<Canvas withToolbar>
    <Story
        name="Disable number input"
        args={{
            isDisabled: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Invalide numberInput

<Canvas withToolbar>
    <Story
        name="Invalid numberInput"
        args={{
            invalidText: 'Invalid text',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Input value with Space

<Canvas withToolbar>
    <Story
        name="Input value with Space"
        args={{
            isCurrency: true,
            value: '123456789',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Input value without Space

<Canvas withToolbar>
    <Story
        name="Spaceless input value"
        args={{
            value: '123456789',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Text Alignment Right

<Canvas withToolbar>
    <Story
        name="Alignment Right"
        args={{
            textAlign: 'right',
            value: '123456789',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Text Alignment Center

<Canvas withToolbar>
    <Story
        name="Alignment Center"
        args={{
            textAlign: 'center',
            value: '123456789',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

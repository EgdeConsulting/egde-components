import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DocsPageHeader, DocsPageBodyComponents } from 'Docs';
import { LibPath } from 'Types';
import { useState } from 'react';
import { NumberInput } from 'Components';

export const Template = (args) => {
    const [value, setValue] = useState('');
    const handleChange = (e) => {
        setValue(e);
    };
    return (
        <div>
            <NumberInput {...args} value={value} onChange={handleChange} />
        </div>
    );
};

export const componentName = 'NumberInput';

<Meta
    title={LibPath.Form + componentName}
    component={NumberInput}
    args={{
        label: 'Title',
        labelTextMargin: '10px',
        value: '',
        maxValueLength: 15,
        onChange: () => {},
        invalidText: '',
        isDisabled: false,
        placeholder: 'Placeholder',
        captionText: 'Caption text',
        captionTextMargin: '10px',
        isCurrency: false,
        currencySpacer: ' ',
        margin: '10px',
        padding: '0',
        width: '100%',
        minWidth: '0',
        backgroundColor: '#ffffff',
        autoFocus: false,
        textAlign: 'left',
        allowNegative: false,
        maxWidth: '100%',
        maxValue: '10',
    }}
/>

<DocsPageHeader
    title={componentName}
    description="This component is used to create an input which accepts numbers only."
    addImportantNote={false}
    variant="stable"
/>

<Canvas withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Canvas>

<DocsPageBodyComponents addStories={true} />

### Disabled NumberInput

<Canvas withToolbar>
    <Story
        name="Disabled NumberInput"
        args={{
            isDisabled: true,
            label: 'Enter your age:',
            placeholder: 'This input is not available yet!',
            backgroundColor: '#F9E5E5',
            captionText: '',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Invalid NumberInput

<Canvas withToolbar>
    <Story
        name="Invalid NumberInput"
        args={{
            label: 'Enter your phone number:',
            invalidText: 'This phone number is aleady in use!',
            value: '36042069',
            captionText: '',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Currency NumberInput

<Canvas withToolbar>
    <Story
        name="Currency NumberInput"
        args={{
            label: 'Enter the amount to transfer:',
            isCurrency: true,
            value: '999999999999',
            captionText: "Woah! That's a lot of money, are you sure?",
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Text Alignment: Right

<Canvas withToolbar>
    <Story
        name="Text Alignment: Right"
        args={{
            label: 'Enter your age:',
            textAlign: 'right',
            value: '123456789',
            captionText: '',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

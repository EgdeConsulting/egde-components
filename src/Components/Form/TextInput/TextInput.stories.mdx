import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DocsPageHeader, DocsPageBodyComponents } from 'Docs';
import { LibPath } from 'Types';
import { useState } from 'react';
import { TextInput } from 'Components';

export const Template = (args) => {
    const [value, setValue] = useState('');
    const handleChange = (e) => {
        setValue(e);
    };
    return (
        <div>
            <TextInput {...args} value={value} onChange={handleChange} />
        </div>
    );
};

export const componentName = 'TextInput';

<Meta
    title={LibPath.Form + componentName}
    component={TextInput}
    args={{
        label: 'Title',
        labelTextMargin: '10px',
        placeholder: 'Placeholder',
        onChange: () => {},
        value: '',
        invalidText: '',
        captionText: 'Caption text',
        isDisabled: false,
        margin: '5px',
        padding: '0',
        width: '100%',
        minWidth: '0',
        backgroundColor: 'transparent',
        autoFocus: false,
        textAlign: 'left',
        characterLimit: 100,
        captionTextMargin: '10px',
        maxWidth: '100%',
    }}
/>

<DocsPageHeader
    title={componentName}
    description="This component is used to create an input which accepts text (numbers, letters and special characters) in a short and static textarea."
    addImportantNote={false}
    variant="stable"
/>

<Canvas withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Canvas>

<DocsPageBodyComponents addStories={true} />

### Disabled TextInput

<Canvas withToolbar>
    <Story
        name="Disabled TextInput"
        args={{
            isDisabled: true,
            label: 'Enter your name:',
            placeholder: 'This input is not available yet!',
            backgroundColor: '#F9E5E5',
            captionText: '',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Invalid TextInput

<Canvas withToolbar>
    <Story
        name="Invalid TextInput"
        args={{
            label: 'Enter your address:',
            invalidText: 'This address is outside Norway!',
            value: '43 Curlew St, London SE1 2ND, Storbritannia',
            captionText: '',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Text Alignment: Center

<Canvas withToolbar>
    <Story
        name="Text Alignment: Center"
        args={{
            textAlign: 'center',
            value: 'Hello World!',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

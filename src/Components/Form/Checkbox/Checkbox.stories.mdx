import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DocsPageHeader, DocsPageBodyComponents } from 'Docs';
import { FormComponentDemo } from './FormComponentDemo';
import { LibPath } from 'Types';
import { Checkbox } from 'Components';
import { useState } from 'react';
import action from '@storybook/addon-actions';

export const Template = (args) => <Checkbox {...args} />;

export const componentName = 'Checkbox';

<Meta
    title={LibPath.Form + componentName}
    component={Checkbox}
    args={{
        direction: 'column',
        options: ['Option One', 'Option Two', 'Option Three'],
        value: [0],
        invalidText: '',
        isDisabled: false,
        backgroundColor: '#ffffff',
        autoFocus: false,
        label: 'Checkbox',
        labelTextMargin: '10px',
        captionText: '',
        captionTextMargin: '0px',
        margin: '0px',
        padding: '0px',
        width: '100%',
        minWidth: '0px',
        maxWidth: '100%',
    }}
    argtypes={{
        onChange: {
            table: {
                category: 'Actions',
            },
        },
    }}
    decorators={[
        (Story, { args }) => (
            <FormComponentDemo
                initialValue={args.values}
                property="values"
                type="checkbox"
            >
                {Story()}
            </FormComponentDemo>
        ),
    ]}
/>

<DocsPageHeader
    title={componentName}
    description="This component is used to create a checklist where multiple answers can be checked off."
    addImportantNote={false}
    variant="stable"
/>

<Canvas withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Canvas>

<DocsPageBodyComponents addStories={true} />

### Checkbox Row

<Canvas withToolbar>
    <Story
        name="Checkbox Row"
        args={{
            direction: 'row',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Invalide Checkbox

<Canvas withToolbar>
    <Story
        name="Invalid Checkbox"
        args={{
            invalidText: 'Invalid',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Disabled Checkbox

<Canvas withToolbar>
    <Story
        name="Disabled Checkbox"
        args={{
            isDisabled: true,
            value: [],
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Multiple Checked

<Canvas withToolbar>
    <Story
        name="Multiple Checked"
        args={{
            value: [0, 1, 2],
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DocsPageHeader, DocsPageBody } from 'Docs';
import { LibPath } from 'Types';
import { MultiSelect } from 'Components';
import { useState } from 'react';

export const Template = (args) => {
    const [value, setValue] = useState();
    const handleChange = (e) => {
        setValue(e);
    };
    return (
        <div>
            <MultiSelect {...args} value={value} onChange={handleChange} />
        </div>
    );
};

export const componentName = 'MultiSelect';

<Meta
    title={LibPath.Form + componentName}
    component={MultiSelect}
    args={{
       options: [{ label: 'Option 1', value: 1 }, { label: 'Option 2', value: 2 }, { label: 'Option 3', value: 3 }, { label: 'Option 4', value: 4 }, { label: 'Option 5', value: 5 }],
        label: 'Select Options',
        labelTextMargin: '0px',
        invalidText: '',
        captionText: '',
        filterText: '',
        submitText: 'Submit',
        cancelText: 'Cancel',
        onChange: () => {},
        isDisabled: false,
        placeholder: 'Select multiple options',
        value: '',
        margin: '0px',
        padding: '0px',
        width: '100%',
        minWidth: '0px',
        maxWidth: '100%',
        autoFocus: false,
        captionTextMargin: '0px',
    }}
/>

<DocsPageHeader
    title={componentName}
    description="This component is used "
    addImportantNote={false}
    variant="stable"
/>

<Canvas withToolbar>
    <Story name="Base" height='300px'>{Template.bind({})}</Story>
</Canvas>

<DocsPageBody addStories={false} />


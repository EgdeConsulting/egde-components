import { Meta, Story, Props, Canvas, Preview } from '@storybook/addon-docs';
import { DocsPageHeader, DocsPageBody } from 'Docs';
import { BaseInput } from './BaseInput';
import { LibPath } from 'Types';

export const Template = (args) => <BaseInput {...args} />;
export const componentName = 'BaseInput';

<Meta
  title={LibPath.Form + componentName}
  component={BaseInput}
  parameters={{
    toolbar: {
      title: { hidden: true },
    },
  }}
  args={{
    label: 'It works',
    labelTextMargin: '0',
    children: [<div>It works</div>],
    invalidText: '',
    captionText: '',
    captionTextMargin: '0',
    margin: '0',
    padding: '0',
    width: '100%',
    minWidth: '0',
    maxWidth: '100%',
  }}
/>

<DocsPageHeader
    componentName={componentName}
    componentDescription="This component is used as a base for other input component such as numberInput, TextInput and FileInput."
    addImportantNote={false}
    variant="stable"
/>

<Preview withToolbar>
  <Story name="BaseInput">{Template.bind({})}</Story>
</Preview>

<Props story="BaseInput" />

<DocsPageBody addStories={false} />

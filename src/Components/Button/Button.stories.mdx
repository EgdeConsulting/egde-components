import { Meta, Story, Props, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { Button } from './Button';
import { LibPath } from 'Types';

export const Template = (args) => <Button {...args} />;

<Meta
  title={LibPath.Buttons + 'Button'}
  component={Button}
  parameters={{
    toolbar: {
      title: { hidden: true },
      // zoom: { hidden: true },
      // eject: { hidden: true },
      // copy: { hidden: true },
      // fullscreen: { hidden: true },
      // 'storybook/background': { hidden: true },
      // 'storybook/viewport': { hidden: true },
    },
  }}
    args={{
        label: 'Button',
        variant: 'primary',
        onClick: () => {}
    }}
/>

# Button

<div
  style={{
    borderRadius: '999999px',
    display: 'inline-block',
    padding: '2px 8px',
    fontSize: '14px',
    lineHeight: '20px',
    fontWeight: '700',
    textAlign: 'center',
    letterSpacing: '0.25px',
    backgroundColor: '#138849',
    color: '#ffffff',
    marginBottom: '16px',
    marginRight: '3px',
  }}
>
  Stable
</div>

<div
  style={{
    borderRadius: '999999px',
    display: 'inline-block',
    padding: '2px 8px',
    fontSize: '14px',
    lineHeight: '20px',
    fontWeight: '700',
    textAlign: 'center',
    letterSpacing: '0.25px',
    backgroundColor: '#D2CB3F',
    color: '#ffffff',
    marginBottom: '16px',
    marginRight: '3px',
  }}
>
  Unstable
</div>

<div
  style={{
    borderRadius: '999999px',
    display: 'inline-block',
    padding: '2px 8px',
    fontSize: '14px',
    lineHeight: '20px',
    fontWeight: '700',
    textAlign: 'center',
    letterSpacing: '0.25px',
    backgroundColor: '#D23F47',
    color: '#ffffff',
    marginBottom: '16px',
    marginRight: '3px',
  }}
>
  Deprecated
</div>

<hr />

<Preview withToolbar>
  <Story name="Primary Button">{Template.bind({})}</Story>
</Preview>



<Props story="Primary Button" />

# Stories

<hr />

Look at some example usecases

### Secondary Button

<Canvas withToolbar>
  <Story
    name="Secondary Button"
    args={{
      variant: 'secondary',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Delete Button

<Canvas withToolbar>
  <Story
    name="Delete Button"
    args={{
      variant: 'delete',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Icon Button

<Canvas withToolbar>
  <Story
    name="Icon Button"
    args={{
      variant: 'iconButton',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Icon and text Button

<Canvas withToolbar>
  <Story
    name="icon And Text Button"
    args={{
      variant: 'iconAndTextButton',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Link Button

<Canvas withToolbar>
  <Story
    name="Link Button"
    args={{
      variant:'link',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
import {
    Meta,
    Story,
    Props,
    Canvas,
    Preview,
} from '@storybook/addon-docs/blocks';
import { Button } from './Button';
import { ComponentStability } from 'Icons';
import { LibPath } from 'Types';

export const Template = (args) => <Button {...args} />;

<Meta
    title={LibPath.Form + 'Button'}
    component={Button}
    args={{
        label: 'Button',
        variant: 'primary',
        onClick: () => {},
    }}
/>

# Button

<ComponentStability variant="stable" />

<hr />

This component is used to create a button which the user can click in order to initiate an action.

<Preview withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Preview>

Change some of the props and see how the `Base` component changes.

<Props story="Base" />

# Stories

<hr />

Below are shown different implementations of the component in regards to varying
use cases.

<br />

### Secondary Button

<Canvas withToolbar>
    <Story
        name="Secondary Button"
        args={{
            variant: 'secondary',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Delete Button

<Canvas withToolbar>
    <Story
        name="Delete Button"
        args={{
            variant: 'delete',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Icon Button

<Canvas withToolbar>
    <Story
        name="Icon Button"
        args={{
            variant: 'iconButton',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Icon and Text Button

<Canvas withToolbar>
    <Story
        name="Icon and Text Button"
        args={{
            variant: 'iconAndTextButton',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Link Button

<Canvas withToolbar>
    <Story
        name="Link Button"
        args={{
            variant: 'link',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

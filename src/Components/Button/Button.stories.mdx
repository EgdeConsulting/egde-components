import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { Button } from './Button';
import { DocsPage } from 'Components';
import { LibPath } from 'Types';

export const Template = (args) => <Button {...args} />;

export const componentName = 'Button';
export const storyNames = [
    'Secondary Button',
    'Delete Button',
    'Icon Button',
    'Icon and Text Button',
    'Link Button',
];

<DocsPage
    componentName={componentName}
    componentDescription="This component is used to create a button which the user can click in order to initiate an action."
    addStories={true}
    storyNames={storyNames}
    addImportantNote={false}
    variant="stable"
/>

<Meta
    title={LibPath.Form + componentName}
    component={Button}
    args={{
        label: 'Button',
        variant: 'primary',
        onClick: () => {},
    }}
/>

<Canvas withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Canvas>

<Canvas withToolbar>
    <Story
        name="Secondary Button"
        args={{
            variant: 'secondary',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas withToolbar>
    <Story
        name="Delete Button"
        args={{
            variant: 'delete',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas withToolbar>
    <Story
        name="Icon Button"
        args={{
            variant: 'iconButton',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas withToolbar>
    <Story
        name="Icon and Text Button"
        args={{
            variant: 'iconAndTextButton',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas withToolbar>
    <Story
        name="Link Button"
        args={{
            variant: 'link',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DocsPageHeader } from 'Docs';
import { Root } from 'Types';
import * as stories from './Registration';

export const templateName = 'Registration';

<Meta title={Root.Templates + templateName} />

<DocsPageHeader
    title={templateName}
    description="This component shows how you can use the component HorizontalStepper to create a registration form for a new user. 
    Step 1 shows a regular contact form using textInput, numberinput and TextAreaInput. Step 2 is using the fileinput component to
    show how a user can add a profile picture. Step 3 uses a Radio component to choose the gender. Step 4 uses checklist and select comopnent to
    add interests for the user and the ability to choose wether or not the user want to receive newsletter"
    addImportantNote={false}
    variant="none"
/>

<Canvas withToolbar>
    <Story name="Registration" story={stories.Base} />
</Canvas>

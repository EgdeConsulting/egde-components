import {
    Meta,
    Story,
    Props,
    Canvas,
    Preview,
} from '@storybook/addon-docs/blocks';
import { Alert } from './Alert';
import { ComponentStability } from 'Icons';
import { LibPath } from 'Types';

export const Template = (args) => <Alert {...args} />;

<Meta
    title={LibPath.Feedback + 'Alert'}
    component={Alert}
    args={{
        description: 'This is an info alert',
        status: 'info',
        linkText: '',
        linkHref: '',
        onClose: () => {},
        hasNoCloseButton: false,
        route: '',
    }}
/>

# Alert

<ComponentStability variant="stable" />

<hr />

This component is used to display a info-box which displays important information
whom the user must see.

<Preview withToolbar>
    <Story name="Base">{Template.bind({})}</Story>
</Preview>

Change some of the props and see how the `Base` component changes.

<Props story="Base" />

# Stories

<hr />

Below are shown different implementations of the component in regards to varying
use cases.

<br />

### Warning Alert

<Canvas withToolbar>
    <Story
        name="Warning Alert"
        args={{
            description: 'This is a warning alert',
            status: 'warning',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Error Alert

<Canvas withToolbar>
    <Story
        name="Error Alert"
        args={{
            description: 'This is an error alert',
            status: 'error',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Success Alert

<Canvas withToolbar>
    <Story
        name="Success Alert"
        args={{
            description: 'This is a Success alert',
            status: 'success',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Alert with link

<Canvas withToolbar>
    <Story
        name="Alert with link"
        args={{
            description: 'This is an info alert',
            status: 'info',
            linkText: 'Learn more',
            linkHref: 'https://www.google.com',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

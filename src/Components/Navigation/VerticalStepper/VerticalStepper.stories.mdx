import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DocsPageHeader, DocsPageBodyComponents } from 'Docs';
import { LibPath } from 'Types';
import { VerticalStepper } from 'Components';
import * as PropItems from './PropItems';
import { useState } from 'react';
import { Button } from '@chakra-ui/react';

export const Template = (args) => {
    const [activeStep, setActiveStep] = useState(0);
    return (
        <div>
            <VerticalStepper
                {...args}
                setActiveStep={setActiveStep}
                activeStep={activeStep}
            />
        </div>
    );
};

export const componentName = 'VerticalStepper';

<Meta
    title={LibPath.Navigation + componentName}
    component={VerticalStepper}
    args={{
        activeStep: 0,
        setActiveStep: () => {},
        stepsContent: PropItems.Default,
        buttonTexts: {
            previous: 'Previous',
            next: 'Next',
            complete: 'Complete',
        },
        finalStep: {
            finalText: 'Final Step',
            finalButtonText: 'Finish',
        },
        completedSteps: [],
        isCompleteButtonDisabled: false,
        buttonCaption: {
            text: 'Caption:',
            color: '#000000',
        },
    }}
      argtypes={{
        onFinalize: { action: 'Finalized' },
        onFinalStep: { action: 'FinalStep' },
    }}
/>

<DocsPageHeader
    title={componentName}
    description="This component is used as navigation between steps."
    addImportantNote={false}
    variant="stable"
/>

<Canvas withToolbar>
    <Story
        name="Base"
        height="300px"
        args={{
            isCompleteButtonDisabled: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<DocsPageBodyComponents addStories={true} />

### Completed Steps

<Canvas withToolbar>
    <Story
        name="Completed Steps"
        height="300px"
        args={{
            activeStep: 4,
            completedSteps: [0, 1, 2, 3, 4],
            isCompleteButtonDisabled: false,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Many Steps

<Canvas withToolbar>
    <Story
        name="Many Steps"
        height="430px"
        args={{
            stepsContent: PropItems.ManySteps,
            isCompleteButtonDisabled: true,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

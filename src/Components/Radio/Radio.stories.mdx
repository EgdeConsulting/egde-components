import { Meta, Story, Props, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { Radio } from './Radio';
import { LibPath } from 'Types';

export const Template = (args) => <Radio {...args} />;

<Meta
  title={LibPath.Form + 'Radio'}
  component={Radio}
  parameters={{
    toolbar: {
      title: { hidden: true },
      // zoom: { hidden: true },
      // eject: { hidden: true },
      // copy: { hidden: true },
      // fullscreen: { hidden: true },
      // 'storybook/background': { hidden: true },
      // 'storybook/viewport': { hidden: true },
    },
  }}
  args={{
    options: ['option 1', 'option 2', 'option 3'],
    value: 0,
    onChange: () => {},
    isDisabled: false,
    backgroundColor: '#fff',
    direction: 'column',
    autoFocus: false,
    label: 'Radio',
    labelTextMargin: '0',
    invalidText: '',
    captionText: '',
    captionTextMargin: '0',
    margin: '0',
    padding: '0',
    width: '100%',
    minWidth: '0',
    maxWidth: '100%',
  }}
/>

# Radio

<div
  style={{
    borderRadius: '999999px',
    display: 'inline-block',
    padding: '2px 8px',
    fontSize: '14px',
    lineHeight: '20px',
    fontWeight: '700',
    textAlign: 'center',
    letterSpacing: '0.25px',
    backgroundColor: '#138849',
    color: '#ffffff',
    marginBottom: '16px',
    marginRight: '3px',
  }}
>
  Stable
</div>

<div
  style={{
    borderRadius: '999999px',
    display: 'inline-block',
    padding: '2px 8px',
    fontSize: '14px',
    lineHeight: '20px',
    fontWeight: '700',
    textAlign: 'center',
    letterSpacing: '0.25px',
    backgroundColor: '#D2CB3F',
    color: '#ffffff',
    marginBottom: '16px',
    marginRight: '3px',
  }}
>
  Unstable
</div>

<div
  style={{
    borderRadius: '999999px',
    display: 'inline-block',
    padding: '2px 8px',
    fontSize: '14px',
    lineHeight: '20px',
    fontWeight: '700',
    textAlign: 'center',
    letterSpacing: '0.25px',
    backgroundColor: '#D23F47',
    color: '#ffffff',
    marginBottom: '16px',
    marginRight: '3px',
  }}
>
  Deprecated
</div>

<hr />

<Preview withToolbar>
  <Story name="Radio">{Template.bind({})}</Story>
</Preview>

<Props story="Radio" />

# Stories
